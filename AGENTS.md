# 開発ガイド

- 目的：作ることを楽しみ、自分で日常利用しながら改善できる小さな公開ツール。
- SimpleMultiStopwatchは参照専用。変更・pushしない。作業先はMulti-Stopwatchのみ。
- 基本役割：ChatGPTはディレクター兼レビュー、Claudeはコーダー兼デザイナー。固定的な制限ではないが、相手の担当を完了したことにしない。
- 初期基盤はChatGPTが作成済み。次の担当と受け入れ条件はdocs/handoff.mdを見る。
- 各担当は既存のbranch/PRとdocs/handoff.mdを読み、変更内容・検証・次の担当を更新する。
- 作業はbranch + PR。レビュー結果をPR本文またはdocs/handoff.mdに残す。マージ・公開はユーザーの指示に従う。
- npm test。計測復元・オフライン・保存失敗に影響する変更には回帰検証を行う。
- 配信はpublic/のみ。外部CDN、追跡、アカウント、サーバー依存を初版へ追加しない。
- 計測ロジック(model.js)、永続化(storage.js)、UI(app.js/style.css)を分離。表示更新回数を経過時間の真実にしない。
- 保存schemaの変更には移行を用意する。読めないデータの無断初期化は禁止。
- PWAの配信ファイルを変えたらsw.jsのVERSIONとキャッシュ対象を更新する。計測中の強制リロードをしない。
- 日本語UI、スマホ、キーボード操作、ラベル付きボタン。隠れたジェスチャーを唯一の操作手段にしない。
