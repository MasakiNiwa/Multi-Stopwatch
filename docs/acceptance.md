# 検証記録と公開前の受け入れ条件

## 実行済み

2026-09-13：Node.js 24 `npm test`、6テスト成功。

- 開始→停止→再開、二重開始の無害性、リセット停止
- 保存→24時間後の読み込みで経過時間復元（tickに非依存）
- 時刻逆行の負数防止と上限
- 複数計測の独立性
- 壊れたJSON、未知schema、重複ID、無効数値の拒否
- 保存失敗の呼び出し元への伝播

構文チェック：app.js。

ブラウザ検証は未実施。環境にChromiumがなく、ダウンロードもタイムアウトしたため、見た目・実際のSW起動・インストールは未検証。

## 公開前チェック（未完了の項目を推測で合格にしない）

- [ ] モバイル390px/320px・PC1280px、長い名前/メモ/10000時間超の表示
- [ ] キーボードだけで追加・編集・停止。ダイアログ終了後のフォーカス
- [ ] 計測中にリロード・タブを閉じる・端末ロック→再開して閉じていた時間を反映
- [ ] 一括停止、並べ替え、リセット/削除の確認とキャンセル
- [ ] バックアップ→復元。壊れたJSONや未知schemaは現在データを保持
- [ ] 保存容量不足・保存不可を再現し、未保存状態を成功扱いしない
- [ ] 複数タブ/インストール画面同時起動で後発画面が閲覧専用
- [ ] `/Multi-Stopwatch/` 配下でmanifest・アイコン・全モジュールのパス正常
- [ ] Android Chromeでインストール→機内モード→完全に閉じて再起動→計測
- [ ] iOS Safari対応を表明する前にホーム画面追加と再起動を実機確認
- [ ] オフライン準備完了前の表示、初回読込み失敗時のメッセージ
- [ ] 新版SW待機中に旧版で計測→全画面閉じる→新版起動、記録保持
- [ ] 他のGitHub Pagesアプリのキャッシュを削除しない

日付・環境・結果を追記し、実機未検証を明示してレビュー依頼する。
