# 検証記録と公開前の受け入れ条件

## 実行済み

### v0.7 / ChatGPT

- Node.js 24の単体テスト42件成功（既存32＋親子・移行・保存失敗10）
- Chromium 141で既存＋v0.7ブラウザテスト成功。通常一覧は390×844で5行、最小44pxの操作領域
- v1/v2→v3移行、親の派生合計と二重計上防止、排他切替、稼働中の子移動、削除と昇格、兄弟sort、グループ継承、構造validationを単体検証
- 親子作成、切替、前回再開、折りたたみと再読込、セット統計、直接削除とUndo、次操作後Undo無効、所属変更、オフライン再起動、別タブの削除禁止をブラウザ検証
- 320/390/844/1280pxで横スクロールなし。light/darkの画像を目視確認。閉じたヘルプが非表示である回帰テストも維持
- 10セット＋50計測の折りたたみ表示で390×844に4親行以上。一致する子を検索すると親の文脈を保持
- 日本語フォントは検証環境のみへ導入。アプリは引き続きシステムフォントのみ、外部CDNなし
- 実機タッチ、OSホーム画面、VoiceOver/TalkBackは未検証。Pages公開後にユーザーとClaudeで確認する

### 2026-09-13 / ChatGPT

Node.js 24 `npm test`、6テスト成功。app.jsの構文チェック。ブラウザ検証は環境にChromiumが無く未実施。

### 2026-09-13 / Claude

Node.js 22 `npm test`、10テスト成功（既存6件＋経過時間の修正・表示・集計の4件）。

実ブラウザ検証：Chromium 141（Playwright 1.56、ヘッドレス、`http://127.0.0.1:8080/Multi-Stopwatch/`）。
公開時と同じサブパス配下で、相対パスとService Workerを含めて確認した。全シナリオでconsoleエラー・pageerror・リクエスト失敗なし。

- [x] モバイル390px/320px・PC1280px。横スクロールなし（overflow 0px）。ライト/ダーク両方
- [x] 長い名前（73文字・空白なしの英単語を含む）、長いメモ、12345時間の表示。カードは3行/4行で切り詰め、全文は`title`と編集画面で参照
- [x] キーボードだけで追加・編集・開始。ダイアログを開くと名前欄にフォーカス、保存後は対象カードの開始ボタンへ移動
- [x] 確認ダイアログのEscキャンセルで記録が残る。削除後のフォーカスは「計測を追加」へ
- [x] 計測中にリロード／ウィンドウを閉じて開き直す→閉じていた時間を反映して継続
- [x] 一括停止（件数を通知）、並べ替え（移動後もキーボード位置を保持）、リセット/削除の確認とキャンセル
- [x] バックアップ→復元。壊れたJSON・未知schemaは現在の記録を保持して拒否
- [x] 保存失敗（QuotaExceededErrorを注入）→画面に反映せず、失敗を明示
- [x] 読めない保存データ→閲覧専用。復元用の「データとインストールについて」を自動で開き、元データの書き出しが可能
- [x] 2つ目のウィンドウは閲覧専用。1つ目は編集を継続できる。閲覧専用でもバックアップは取得可能
- [x] `/Multi-Stopwatch/` 配下でmanifest・アイコン・全モジュールが200。start_url/scopeも同配下に解決
- [x] オフライン準備完了の表示。通信を切ってリロードしても起動し、計測が継続
- [x] キャッシュ外のサブパスへの遷移はアプリ本体へフォールバック（オフライン時も200）
- [x] 新版SW待機中に旧版で計測→全ウィンドウを閉じる→新版で起動、記録と計測を保持
- [x] 同一オリジンの他アプリのキャッシュを削除しない（別scope・無関係名の両方が残存）

### 2026-09-13 / Claude（v0.4 コンパクト一覧とテーマ選択）

`npm test` 15件成功。`npm run test:browser` 成功（CIでも実行）。
実ブラウザ：Chromium 141（Playwright、ヘッドレス、`http://127.0.0.1:8080/Multi-Stopwatch/`）。全シナリオでconsoleエラー・pageerrorなし。

一覧の密度（390×844、固定ツールバーに隠れない範囲で数えた完全表示の行数）

| 件数 | 行の高さ | 完全に見える行 | ページ全体 |
|---|---|---|---|
| v0.3 カード 6件 | 297px | 1件 | 2397px |
| v0.4 行 4件 | 66px | **4件**（スクロールなし） | 844px |
| v0.4 行 10件 | 66px | **6件** | 1270px |

- [x] 390×844で4件以上の名前・時間・開始停止を過度なスクロールなしに把握できる（4件はスクロールなしで全件）
- [x] 10件から目的の計測を絞り込みで特定できる（「ピアノ」で1件、「10件中1件を表示」）
- [x] 並べ替え：ドラッグ、上下キー、詳細画面のボタンの3経路。いずれも再読込後に順序を維持
- [x] 上下キーは連続で押しても1回ずつ確実に動き、フォーカスがハンドルに残る
- [x] 絞り込み中はハンドルを無効化し、理由を表示
- [x] タップ領域は最小44px（ボタン・テーマ選択・検索欄を全走査して計測）
- [x] キーボードのみで追加・開始・詳細・削除の取り消しまで到達。読み上げラベルと移動の読み上げ通知あり
- [x] テーマ3種の切替、再読込後の維持、新しいウィンドウでの維持（PWA再起動相当）
- [x] 明示的な選択が端末設定より優先される。端末設定モードではOSの変更に即追従
- [x] `<meta name="theme-color">` が選択テーマに追従（ライト #f3f5f9 / ダーク #0b0e14）
- [x] 計測中にテーマを変更しても計測が継続し、再読込後も継続
- [x] 設定の保存に失敗しても表示は切り替わり、計測データには影響しない
- [x] 保存失敗（QuotaExceededError注入）で状態を更新せず、失敗を明示
- [x] 2つ目のウィンドウは閲覧専用。詳細画面は読めるが操作ボタンは無効。テーマ切替は可能
- [x] オフライン起動、範囲内の未知パスのフォールバック、バックアップ／復元、壊れたファイルの拒否
- [x] 新版SW待機中に旧版で計測→全ウィンドウを閉じる→新版で起動。記録・計測・テーマを保持し、他アプリのキャッシュは残る
- [x] 320px・1280pxで横スクロールなし

### 2026-09-13 / Claude（v0.5 グループ・一括並べ替え・統計）

`npm test` 31件成功（既存15件＋schema/移行6件、並べ替え6件、集計4件）。`npm run test:browser` 成功（CIでも実行）。
実ブラウザ：Chromium 141（Playwright、ヘッドレス、`http://127.0.0.1:8080/Multi-Stopwatch/`）。全シナリオでconsoleエラー・pageerrorなし。

schemaと移行

- [x] v0.4が実際に書く形のv1レコードをブラウザのlocalStorageに置いて起動し、時間・稼働中・順序・名前・目標を保ったままv2で表示
- [x] 読み込んだだけでは記録を書き換えず（保存値はv1のまま）、次の保存でv2になる。`groupId`は全件null、`groups`は空
- [x] 未知の版（v3）・壊れたJSON・配列などは例外にして閲覧専用へ落とす（黙って初期化しない）
- [x] 書き出しはv2。読み込みはv1バックアップ／v2バックアップの双方を受け付ける
- [x] 存在しないグループidを指すタイマーは`validate`が拒否する

グループ

- [x] 追加・改名・削除。同名（全角半角・大文字小文字の違いを含む）と空名、上限20件を拒否
- [x] 削除は確認ダイアログ付き。所属していた計測は消えず「未分類」へ移り、時間と順序を保つ
- [x] 一覧の行、詳細画面、編集画面、統計にグループが反映される

一括並べ替え

- [x] 11条件すべてが件数を保って並べ替える（重複・欠落なし、入力を変更しない）
- [x] 同値は適用前の順序を保つ（昇順・降順の両方向で確認）
- [x] 累計時間順は稼働中の現在値を含む
- [x] 未分類はグループ名順の最後、目標なしは進捗順の昇順・降順とも最後
- [x] 適用結果を保存し、再読込後も維持。aria-liveで件数と条件を通知

統計

- [x] グループ別小計の合計が全体合計と一致し、割合の合計が1になる
- [x] 0件・全部0秒でも割合がNaN/Infinityにならず0%になり、同着は同順位
- [x] 定義済みで0件のグループは残り、未分類は所属がある時だけ出る
- [x] 稼働中は表示が更新される（1秒ごとに再描画、変化がなければ描き直さない）

レイアウトとアクセシビリティ

- [x] 390×844：タブで計測／統計を切替。10件登録時に一覧は5件、CIの構成では8件が固定ツールバーの上に収まる
- [x] 1280×900：一覧と統計を左右に常時表示、タブは非表示。統計はsticky
- [x] 844×390：2カラムのまま操作可能。ヘッダーを縮め、統計と重複するヒーローの合計を隠して一覧に高さを回す
- [x] 320px／390px／844px／1280pxで横スクロールなし
- [x] タップ領域は最小44px（ボタン・タブ・テーマ選択・並べ替え・絞り込みを全走査）
- [x] タブは左右キーで移動でき、選択タブだけがタブ順に入る
- [x] ライト／ダーク両方をスクリーンショットで確認

継続性

- [x] 再読込・オフライン起動でグループ・並び順・テーマを保持
- [x] 計測中のテーマ変更・タブ切替で時間が失われない
- [x] 保存失敗（QuotaExceededError注入）で状態を更新せず、失敗を明示
- [x] 2つ目のウィンドウは閲覧専用のまま

### 2026-09-13 / Claude（v0.5 レビュー指摘の反映）

`npm test` 32件成功。`npm run test:browser` 成功（CIでも実行）。Chromium 141、consoleエラー・pageerrorなし。

- [x] 配色をMaterial 3系のsemantic tokenへ整理。gradient・glow・radial背景・強い影を廃止（影はダイアログのみ）
- [x] ダークはニュートラルグレーのsurface階層。primaryは低彩度の青紫1系統
- [x] コントラスト実測（light/dark各9か所）。すべて4.5:1以上、最小6.63:1
- [x] 計測中を色だけで伝えない（塗りつぶしトグル・文字・動くドット・枠線）
- [x] 数値欄はfocusで全選択し、0を打ち替えられる
- [x] 目標プリセット（なし/15分/30分/1時間/2時間）→時間・分での微調整→プリセット表示の解除
- [x] 経過時間の修正は既定で折りたたみ、要約に現在値。展開して保存できる
- [x] 分の欄を空にしても即エラーにせず、保存時に0として扱う
- [x] 320px/390pxでプリセットが1行に収まり、操作領域44pxを維持
- [x] 統計は行を使い回す差分更新。2秒間の観測で行の作り直し0件、かつ1秒以内に値が更新される
- [x] ヒーローの合計と統計パネルの合計が同時刻で一致する
- [x] 統計タブで固定の操作バーがランキングに重ならない（バーは計測タブにのみ出す）
- [x] 320pxでの横スクロールなし（`.columns` のgridトラックがはみ出す不具合を修正）

### 2026-09-13 / Claude（v0.6 アイコン・1ボタンテーマ・ヘルプ・README）

`npm test` 32件成功。`npm run test:browser` 成功（CIでも実行）。`git diff --check` 問題なし。Chromium 141、consoleエラー・pageerrorなし。

アイコン

- [x] `icon.svg` / `icon-maskable.svg` を原本に、32 / 180 / 192 / 512 と maskable 192 / 512 を生成
- [x] v0.5のタイトル左にあった「円環＋右上の点」をブランドの正とし、ヘッダーと外部アイコンの形を統一
- [x] maskableの図形が安全領域（中心から半径40%）に収まる
- [x] 円形マスク・角丸マスクでの見え方を目視確認（docs/screenshots/icons.png）
- [x] 32pxでも円環と右上の点が潰れず識別できる。文字なし
- [x] favicon・インストール用画像はライトテーマのブランド配色で統一
- [x] `index.html`・manifest・Service Workerの事前キャッシュを更新
- [x] manifestの `name` / `short_name` / Appleタイトルを「Multi Stopwatch」に統一
- [x] 参照するすべてのアイコンURLが200（favicon・32px・Apple・manifest内の全件）
- [x] `purpose: any` と `maskable` が別ファイルを指す

テーマ（1ボタン）

- [x] 保存設定がない初回は端末設定に従う
- [x] 押すたびに表示中の反対（light↔dark）へ切り替わり、明示設定として保存される
- [x] `aria-label`・`title` が現在状態に応じて「ダーク/ライトテーマに切り替える」へ変わる
- [x] 表示アイコンは押した結果（ライト表示中は月、ダーク表示中は太陽）
- [x] 切替をライブリージョンで通知する
- [x] 既存prefs（`system|light|dark`）をそのまま読み、schemaは変更なし
- [x] theme-color・初回描画のちらつき防止・再読込/オフライン後の保持を維持

ヘルプ

- [x] ヘッダーのボタンからダイアログで開き、閉じるボタンとEscapeで閉じる
- [x] 閉じるとフォーカスが開いたボタンへ戻る
- [x] 閉じてもスクロール位置と計測状態を壊さない
- [x] バックアップ保存・復元がダイアログ内にある
- [x] ページ末尾の旧 `details.help` を廃止（二重導線なし）
- [x] 画面高さが低い場合は本文だけがスクロールし、閉じるボタンは常に見える
- [x] 保存データが読めないときは自動で開く

レイアウト・README

- [x] 320×844 / 390×844でヘッダーに横スクロールなし、ボタンは1行、44px以上
- [x] READMEの相対リンク（LICENSE・docs・AGENTS）とスクリーンショットの実在を確認
- [x] Pagesはmain pushで自動公開であることを確認（直近runが成功）

未検証：公開URL `https://masakiniwa.github.io/Multi-Stopwatch/` への到達はこの実行環境のegress制限で確認できていません。Pagesワークフローの成功までを確認しています。

### 2026-09-13 / ChatGPT（v0.6.1 ホットフィックス）

- [x] 初期表示で閉じたヘルプの算出済み `display` が `none`
- [x] 閉じるボタン操作後もヘルプの算出済み `display` が `none`
- [x] `open` 中だけflexレイアウトになり、本文スクロールと固定フッターを維持
- [x] Service Workerのキャッシュ版を更新し、古いCSSを置き換える
- [x] `npm test` 32件成功、`git diff --check` 成功

## 公開前チェック（未完了の項目を推測で合格にしない）

実機とブラウザ固有の挙動は未確認。ヘッドレスChromiumでは再現できない。

- [ ] Android Chromeでインストール→機内モード→完全に閉じて再起動→計測
- [ ] 実機タッチでの並べ替えドラッグの感触（ヘッドレスのPointer Eventsでは代替）
- [ ] インストール済みPWAのアイコン差し替え（OSキャッシュの反映タイミング）
- [ ] iOSのホーム画面追加でのApple touch icon表示
- [ ] 実機の横向き（画面回転による自動レイアウト切替）
- [ ] 実機スクリーンリーダーでのタブと統計の読み上げ
- [ ] インストール済みPWAを完全終了してからのテーマ維持（新規ウィンドウでの確認に留まる）
- [ ] インストール導線（`beforeinstallprompt`とインストールボタン）の実機動作
- [ ] iOS Safari対応を表明する前にホーム画面追加と再起動を実機確認
- [ ] 端末ロック・OSスリープをまたぐ復元
- [ ] 実際の保存容量不足（本検証は例外注入による代替）
- [ ] GitHub Pagesへの配信と公開URLでの初回読込み・オフライン準備
- [ ] 初回読込み失敗時のメッセージ（オフライン準備失敗の実地確認）

日付・環境・結果を追記し、実機未検証を明示してレビュー依頼する。

### 2026-09-13 / ChatGPT v0.3レビュー

Claude版のコード差分、10件のテスト、提出された4画面を確認。機能上の公開阻害事項は見つからなかった。ユーザー評価を反映してビジュアルを再設計したため、変更後のブラウザ検証とスクリーンショット確認をGitHub Actionsのbrowser jobで行う。合格前に公開済みと扱わない。


### 2026-09-13 / ユーザー実機確認

GitHub Pages公開版を実機で起動・操作できた。次の改善課題として、多数のストップウォッチを煩雑に見せず一覧・選択・操作・並べ替えできる密度と、テーマの手動選択を採用した。具体的な端末・ブラウザ別のインストール／機内モード結果は次回Claude PRで追記する。
