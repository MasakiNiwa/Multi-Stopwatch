# 引き継ぎ

更新：2026-09-13 / ChatGPT
次の担当：**Claude（コーダー兼デザイナー）**

## できたこと

元Flutter版のコードを参照し、ES Modulesの計測・保存・仮UI・PWA・CI・手動Pages配信を用意した。旧リポジトリは変更していない。UIは最終デザインではなく、機能契約を動かして確認する初期案。ChatGPTがClaudeの実装を実行したわけではない。

## Claudeへ

1. このPR/ブランチを起点にコードと画面を確認。問題を見つけたら基盤も修正してよい。
2. まず初見ユーザーとして「追加→名前→開始→停止→積み重ねの確認」を評価。スマホで操作が迷わない完成版UIに磨く。
3. 追加直後にキャンセルしても空の計測が残る仕様、編集後のフォーカス、削除確認、空状態、長い名前・長時間表示を見直す。
4. 基本計測・保存・オフラインの検証を優先。グループ等を追加する場合は初版の範囲が膨らまないよう理由を添える。
5. docs/acceptance.mdの残検証を実施し、結果とChatGPTに見てほしい点をこのファイルに追記してPRを作る。

## 依頼時の文章

> Multi-Stopwatchの初期基盤PRを起点に、AGENTS.mdとdocs/handoff.mdを読んでください。あなたはコーダー兼デザイナーです。既存の計測・保存契約を守りながら、スマホで使いやすく美しい初版へブラッシュアップしてください。必要なバグ修正もお願いします。変更はPRにまとめ、ChatGPTのレビュー待ちにしてください。

## リリース判断

公開済みとは扱わない。初回公開前に受け入れ項目と実機インストールを確認する。公開先はGitHub Pages。マージと公開の指示は別途ユーザーに従う。
